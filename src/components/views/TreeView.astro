---
import FamilyTree from "../FamilyTree.tsx";
import { getPeople, getTranslations, toTreeData } from "../../lib/people";
import { useTranslations, localize, type Locale } from "../../i18n/ui";

interface Props {
  lang: Locale;
}
const { lang } = Astro.props;
const t = useTranslations(lang);

const people = await getPeople();
const translations = await getTranslations();
const treeData = toTreeData(people, translations, lang);

const labels = {
  hint: t("tree.hint"),
  zoomIn: t("tree.zoomIn"),
  zoomOut: t("tree.zoomOut"),
  reset: t("tree.reset"),
  expandAll: t("tree.expandAll"),
  collapseAll: t("tree.collapseAll"),
  living: t("tree.living"),
  deceased: t("tree.deceased"),
  showMore: t("tree.showMore"),
};
---

<section
  class="mx-auto flex max-w-[1600px] flex-col gap-3 px-5 pt-10 pb-3 sm:flex-row sm:items-end sm:justify-between"
>
  <div>
    <h1 class="text-3xl font-semibold tracking-tight text-ink">
      {t("tree.title")}
    </h1>
    <p class="mt-2 max-w-2xl text-muted">{t("tree.subtitle")}</p>
  </div>
  <a
    href={localize("/poster.svg", lang)}
    download
    class="inline-flex w-fit items-center gap-2 rounded-xl border border-line bg-surface px-4 py-2 text-sm font-medium text-ink shadow-sm transition hover:border-accent hover:text-accent"
  >
    ↓ {t("tree.poster")}
  </a>
</section>

<section class="mx-auto max-w-[1600px] px-5 pb-12">
  <FamilyTree
    client:load
    people={treeData}
    labels={labels}
    basePath={localize("/people", lang)}
  />
</section>
